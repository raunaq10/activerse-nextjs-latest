# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/activerse
# Or use MongoDB Atlas connection string:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/activerse?retryWrites=true&w=majority

# Session Secret (Generate a random 32+ character string)
SESSION_SECRET=your-super-secret-session-key-change-this-in-production-min-32-chars

# Email Service Configuration (Gmail SMTP - Free)
# Use your Gmail account or create an App Password
# To create App Password: Google Account > Security > 2-Step Verification > App Passwords
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password-here

# Admin Email (Optional - defaults to EMAIL_USER if not set)
ADMIN_EMAIL=admin@activerse.com

# Contact Email (Optional - for contact form)
CONTACT_EMAIL=contact@activerse.com

# Razorpay Payment Gateway Configuration
# Get these from: https://dashboard.razorpay.com/app/keys
# 
# TEST MODE (Current Setup):
# - Go to Razorpay Dashboard > Settings > API Keys > Test Mode
# - Copy the Key ID (starts with rzp_test_) and Key Secret
# - Use test cards for testing (see test cards below)
#
# LIVE MODE (For Production):
# - Activate your Razorpay account
# - Go to Settings > API Keys > Live Mode
# - Copy the Key ID (starts with rzp_live_) and Key Secret
# - Replace test keys with live keys

# Server-side Razorpay Keys (Keep these SECRET - never expose to client)
# TEST MODE KEY (starts with rzp_test_)
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_test_key_secret_here

# Client-side Razorpay Key (Public - safe to expose in browser)
# This MUST be the same as RAZORPAY_KEY_ID but with NEXT_PUBLIC_ prefix
# TEST MODE KEY (starts with rzp_test_)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx

# Base URL (Optional - for email links)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# For production:
# NEXT_PUBLIC_BASE_URL=https://www.activerse.co.in

# Node Environment
NODE_ENV=development
# For production:
# NODE_ENV=production

# ============================================
# RAZORPAY TEST MODE - TEST CARDS
# ============================================
# Use these test cards when testing payments:
#
# SUCCESSFUL PAYMENT:
# Card Number: 4111 1111 1111 1111
# Expiry: Any future date (e.g., 12/25)
# CVV: Any 3 digits (e.g., 123)
# Name: Any name
#
# FAILED PAYMENT:
# Card Number: 4000 0000 0000 0002
# Expiry: Any future date
# CVV: Any 3 digits
# Name: Any name
#
# UPI TEST:
# Use any UPI ID (e.g., test@razorpay)
#
# NETBANKING TEST:
# Select any bank from the list
#
# ============================================
# IMPORTANT NOTES:
# ============================================
# 1. Test mode keys start with "rzp_test_"
# 2. Live mode keys start with "rzp_live_"
# 3. Make sure RAZORPAY_KEY_ID and NEXT_PUBLIC_RAZORPAY_KEY_ID are the SAME
# 4. Test mode does NOT charge real money
# 5. Check payment status at: https://dashboard.razorpay.com/app/payments
